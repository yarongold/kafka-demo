apiVersion: v1
kind: Service
metadata:
  name: consumer
  namespace: kafka-demo
  labels:
    app: consumer
spec:
  type: ClusterIP
  selector:
    app: consumer
  ports:
    - name: http
      port: 8080
      targetPort: http
